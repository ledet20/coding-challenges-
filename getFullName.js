//Given a first and a last name, "getFullName" returns a single string with 
// the given first and last names separated by a single space.

function getFullName(firstName, lastName) {
  return firstName + ' ' + lastName // create a return statement that will output the first and last name
                                    // with a space between the two
}

getFullName('mary', 'kate'); // 'mary kate'
