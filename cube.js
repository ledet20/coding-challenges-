// Given a number, "cube" returns the cube of that number.

function cube(num) {
  return Math.pow(num, 3); // using a javascript built in function Math.pow take two parameters 
                           // the base number and the exponent in which the number will be raised to 
}

cube(2); // 8 
